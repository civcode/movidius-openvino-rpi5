# Python packages used by the OpenVINO build helper environment.
#
# APT provides python3 + python3-venv. Python libraries are isolated in
# /opt/venv. These pins are pure-Python and therefore identical on armv7, arm64 and
# amd64; the OpenVINO CMake build itself does not require NumPy/OpenCV here.
pip==21.3.1
setuptools==59.6.0
wheel==0.37.1

# Model Optimizer dependencies are intentionally handled by
# scripts/prepare-mobilenet.sh in a native host-architecture Python container.
# They are not installed into the armv7/arm64/amd64 inference images.
